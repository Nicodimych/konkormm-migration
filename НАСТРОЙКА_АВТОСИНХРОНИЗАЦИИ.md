# ⚙️ Настройка автоматической синхронизации с GitHub

## 🎯 Что настраиваем

Автоматический `git pull` каждый час (или каждые 30 минут) чтобы не пропустить вопросы от команды.

---

## 📋 Вариант 1: Windows Task Scheduler (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Откройте Task Scheduler

1. Нажмите `Win+R`
2. Введите: `taskschd.msc`
3. Enter

### Шаг 2: Создайте задачу

1. **Action** → **Create Basic Task**
2. **Name:** `Git Sync - Konkormm Migration`
3. **Description:** `Автоматическая синхронизация проекта миграции с GitHub`
4. **Trigger:** Daily
5. **Start:** Сегодня, время: 08:00
6. **Recur every:** 1 days
7. **Advanced settings** → галочка **"Repeat task every"**: **1 hour**
8. **Duration:** 1 day

### Шаг 3: Настройте действие

1. **Action:** Start a program
2. **Program/script:**
   ```
   powershell.exe
   ```
3. **Add arguments:**
   ```
   -ExecutionPolicy Bypass -File "C:\1C\edt\projects\konkormm\auto_sync.ps1"
   ```
4. **Start in:**
   ```
   C:\1C\edt\projects\konkormm
   ```

### Шаг 4: Дополнительные настройки

1. Вкладка **Settings:**
   - ✅ "Run task as soon as possible after a scheduled start is missed"
   - ✅ "Stop the task if it runs longer than: 10 minutes"
   
2. **Finish**

**Готово!** Теперь каждый час автоматически:
- Проверяется GitHub
- Если есть изменения → `git pull`
- Если обновился `QUESTIONS.md` → запись в лог

---

## 📋 Вариант 2: Через настройки Cursor (проще, но менее надёжно)

### Включить Git Autofetch:

1. В Cursor: `Ctrl+,` (Settings)
2. Найти: **Git: Autofetch**
3. Включить (галочка)
4. **Git: Autofetch Period** = `60` (каждую минуту проверяет GitHub)

**Что это даёт:**
- ✅ Cursor показывает что есть новые изменения (иконка со стрелкой в Source Control)
- ⚠️ **Но файлы НЕ обновляются** - нужно вручную нажать "Pull"

**Плюс:** Видите что есть обновления  
**Минус:** Всё равно нужно вручную Pull

---

## 📋 Вариант 3: Простой bat-файл + ярлык (самое простое)

Создам файл который делает pull одной кнопкой:

### Создать файл `sync.bat`:

```batch
@echo off
cd /d C:\1C\edt\projects\konkormm
echo Синхронизация с GitHub...
git pull
echo.
echo Готово! Проверьте QUESTIONS.md на новые вопросы.
pause
```

**Как использовать:**
1. Дважды кликнуть `sync.bat` → автоматический pull
2. Можно создать ярлык на рабочий стол
3. Кликать когда вспомните (утром, вечером)

**Плюс:** Очень просто  
**Минус:** Нужно помнить запускать

---

## 🏆 МОЯ РЕКОМЕНДАЦИЯ:

### Комбинированный подход:

1. **Настроить Autofetch в Cursor** (вариант 2)
   - Будете видеть что есть обновления
   
2. **Создать bat-файл** (вариант 3)
   - Быстрый pull одной кнопкой

3. **Опционально: Task Scheduler** (вариант 1)
   - Если хотите полную автоматизацию

**Workflow:**
- Утром открываете Cursor → видите иконку "есть обновления"
- Кликаете ярлык `sync.bat` → pull
- Проверяете `QUESTIONS.md`

---

## 📊 Что выбираете?

- **A)** Только Autofetch в Cursor (минимум, видите обновления)
- **B)** Autofetch + bat-файл (удобно, pull кнопкой)
- **C)** Task Scheduler (полная автоматизация, каждый час)

Создать bat-файл и настроить Autofetch? (займет 2 минуты)
